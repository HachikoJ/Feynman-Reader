# v0.4.6

本版本修正页脚的版权声明，与仓库中的 MIT `LICENSE` 保持一致。

## 页脚声明

- 中文页脚由「保留所有权利」改为「MIT 开源」。
- 英文页脚由 `All Rights Reserved` 改为 `MIT licensed`。
- 页面右上角的 GitHub 开源入口保持不变。

本版本不涉及数据库结构、账号权限或已保存凭据行为变更。

## English

Version 0.4.6 corrects the footer licence statement so it matches the MIT `LICENSE` in the repository.

The Chinese footer now reads 「MIT 开源」 instead of the previous all-rights-reserved text, and the English footer reads `MIT licensed` instead of `All Rights Reserved`. The GitHub entry point in the top-right corner is unchanged.

No database schema, account permission, or stored credential behavior changes.
